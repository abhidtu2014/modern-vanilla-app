export const AppTemplate = (model) => `
    <section class="app">
        <h3> ${model.title} </h3>
        <section class="button">
            <button class="btn-configure-table"> Configure DataTable </button>
        </section>
    </section>
`
